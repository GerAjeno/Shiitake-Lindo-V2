# Node / Next.js
node_modules/
.next/
out/
build/
dist/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.env
.env.*
!.env.example

# Arduino / PlatformIO / IDEs
*.hex
*.bin
*.elf
.vscode/
.idea/
*.DS_Store
Thumbs.db

# Secretos (nunca deben versionarse)
**/serviceAccountKey*.json
**/firebase-service-account*.json
**/ota-signing-key*
**/*.pem
**/*.key
infra/secrets/

# Postgres data volume si se monta local
infra/postgres/data/

# Build artifacts
*.tsbuildinfo

# Copia local de Shared/ generada por Backend/package.json (predev/prebuild) para que tsc
# no falle con TS6059 (rootDir) — la fuente de verdad sigue siendo /Shared en la raíz.
Backend/src/shared/
