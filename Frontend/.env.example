# El Frontend ya no necesita variables de entorno para autenticación: el login es 100% local
# (cookie httpOnly emitida por el backend, ver Backend/src/auth/local.ts). No hay ningún SDK
# externo ni build args que configurar aquí.
